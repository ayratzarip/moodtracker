# README.md - Bipolar Mood Tracker TMA

## 1. Обзор проекта (Project Overview)
Мы создаем **Telegram Mini App (TMA)** для трекинга настроения пациентов с биполярным аффективным расстройством (БАР).
**Главная цель:** Максимально простой, не отвлекающий интерфейс для ежедневной оценки настроения (шкала от -5 до +5) и заметок.
**Ключевая особенность:** Использование `Telegram CloudStorage` для хранения данных (без собственного БД) и `Google Apps Script` для отправки напоминаний (без полноценного бэкенда).

## 2. Технологический стек (Tech Stack)
Используй этот стек при генерации кода:
*   **Framework:** React (Vite).
*   **Language:** TypeScript.
*   **Styling:** Tailwind CSS (Mobile-first, минимализм).
*   **UI Components:** Shadcn/UI или Radix UI (для слайдеров и модальных окон).
*   **Routing:** React Router DOM (для навигации внизу экрана).
*   **Charts:** Recharts (для визуализации графика).
*   **Telegram SDK:** `@twa-dev/sdk` (или стандартный `window.Telegram.WebApp`).
*   **Backend (Notifications):** Google Apps Script (будет развернут отдельно, приложение просто шлет туда `chat_id` и время).

## 3. Структура данных (Data Structure)
Данные хранятся в **Telegram CloudStorage**.
Чтобы обойти ограничения по количеству ключей, данные группируются по месяцам.

**Формат ключа:** `mood_YYYY_MM` (например, `mood_2023_10`)
**Формат значения (JSON String):**
```json
{
  "2023-10-01": {
    "score": 2,   // Number: -5 to +5
    "note": "Чувствовал подъем сил, но плохо спал.", // String
    "timestamp": 1696156800000
  },
  "2023-10-02": {
    "score": -1,
    "note": "",
    "timestamp": 1696243200000
  }
}
```
**Настройки пользователя (ключ `user_settings`):**
```json
{
  "reminderTime": "20:00", // UTC или локальное время пользователя
  "onboarded": true
}
```

## 4. Навигация и UI (Navigation & UI)
Приложение имеет **Bottom Navigation Bar** (Нижняя панель), доступная на всех экранах, кроме модальных окон ввода.
Активная вкладка подсвечивается цветом (Accent Color из темы Telegram).

**Кнопки навигации:**
1.  🏠 **Главная** (Home)
2.  ⏰ **Напоминания** (Settings/Reminders)
3.  📈 **График** (Chart)
4.  ℹ️ **Инструкция** (Help)

**Дизайн-код:**
*   Чистый, спокойный интерфейс.
*   Адаптация под темную/светлую тему Telegram (`var(--tg-theme-bg-color)` и т.д.).

## 5. Логика экранов (Screen Flows)

### 5.1. Главный экран (Home)
*   **Элемент:** Большая кнопка по центру: "Как прошел сегодняшний день?".
*   **Действие:** При нажатии проверяем `CloudStorage` на наличие записи за сегодня.
    *   *Если записи нет:* Переход на экран "Оценка" (Input Flow).
    *   *Если запись есть:* Показать Alert/Modal: "Оценка за сегодня уже стоит. Хотите изменить?". Если "Да" -> переход на экран "Оценка".

### 5.2. Флоу ввода данных (Input Flow)
Этот процесс разбит на шаги (Wizard-like):

*   **Шаг 1: Оценка (Score)**
    *   Вертикальный или горизонтальный скролл/слайдер.
    *   Диапазон: целые числа от **-5** (Тяжелая депрессия) до **+5** (Мания/Гипомания), включая **0** (Ровное состояние).
    *   Кнопка "Далее" / "Сохранить".
*   **Шаг 2: Примечание (Note)**
    *   Textarea для свободного ввода.
    *   Кнопка "Сохранить" (Save).
*   **Финализация:**
    *   Данные сохраняются в `CloudStorage`.
    *   Пользователь возвращается на Главный экран.

### 5.3. Экран Напоминаний (Settings)
*   При самом первом запуске приложения этот экран показывается принудительно.
*   **UI:** Выбор времени (Time Picker).
*   **Логика:** При сохранении времени, приложение отправляет запрос (fetch) на Google Apps Script Webhook URL. В теле запроса: `{ chatId: TG_USER_ID, time: "HH:MM", timezone: "..." }`.
*   *Примечание для AI:* Сгенерируй моковый URL для GAS, который можно будет заменить в конфиге.

### 5.4. Экран Графика (Chart)
*   **Проверка платформы:** Используем `Telegram.WebApp.platform`.
*   **Мобильная версия:**
    *   Отображается упрощенный график (Recharts LineChart).
    *   Сверху уведомление: *"Для детального анализа и удобного просмотра откройте Telegram на компьютере"*.
*   **Десктопная версия:**
    *   График разворачивается на весь экран.
    *   **Интерактивность:** Hover (наведение мыши) на точку показывает Tooltip с датой, оценкой и текстом **Примечание**.
*   **AI Export (Кнопка "Анализ AI"):**
    *   Расположена под графиком.
    *   **Действие:** Собирает все данные из CloudStorage в один JSON/CSV.
    *   **Результат:** Генерирует и скачивает файл (или копирует в буфер обмена) текст, содержащий данные + Промпт.
    *   **Текст Промпта:**
        > "Я веду дневник настроения для контроля биполярного расстройства. Ниже приведены данные в формате JSON (Дата, Оценка от -5 до +5, Заметка). Проанализируй эти данные. Найди закономерности, триггеры (основываясь на заметках), длительность фаз и дай рекомендации. Данные: [JSON_DATA]"

### 5.5. Экран Инструкций (Help)
*   **Видео:** Embed плеер с Vimeo (используй любое public video ID как заглушку, например `vimeo.com/76979871`).
*   **Текст:** Краткая инструкция:
    1. Настрой время напоминаний.
    2. Каждый день оценивай состояние.
    3. Используй десктоп для анализа графика.
    4. Используй кнопку AI для экспорта данных врачу или в ChatGPT.

## 6. Интеграция с Google Apps Script (Концепт)
*В коде приложения нужен только API Service для вызова вебхука.*
Логика GAS (для справки AI):
*   Скрипт принимает POST запрос с ID пользователя и временем.
*   Скрипт устанавливает Trigger, который раз в сутки отправляет сообщение через Telegram Bot API:
    *   Текст: "Оцени, как прошел сегодняшний день?"
    *   Кнопка (Inline Keyboard): Launch Mini App.

## 7. Инструкция по генерации (Prompt for Cursor/Vibe)
1.  Сначала создай структуру проекта (Vite + React).
2.  Настрой маршрутизацию и базовый Layout с навигацией.
3.  Реализуй сервис для работы с `Telegram.WebApp.CloudStorage` (с учетом группировки по месяцам).
4.  Создай компоненты UI: Slider для ввода, Chart для графика.
5.  Реализуй логику определения платформы (Mobile/Desktop) для Графика.
6.  Собери функционал экспорта для AI.

***

### Файловая структура (Предложение)
```
src/
  components/
    layout/
      BottomNav.tsx
      Header.tsx
    ui/
      MoodSlider.tsx
      ChartDesktop.tsx
      ChartMobile.tsx
      VimeoEmbed.tsx
  pages/
    Home.tsx
    InputWizard.tsx (Step 1 & 2)
    Settings.tsx
    ChartPage.tsx
    Help.tsx
  services/
    storage.ts (CloudStorage wrapper)
    notifications.ts (GAS Webhook wrapper)
    aiExport.ts (Data formatting logic)
  App.tsx
  main.tsx
```